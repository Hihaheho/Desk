{
  @files [
    {
      @name "main.ds",
      @content "
        'type add \\'number, 'number -> @sum 'number
        'string => 'number
          <'a add>
            ! 1 => 'number,
            ! 2 => 'number
        $ ! \"a\" => 'number: 'a x ~
        <'a add> <'a x>, <'a x>
      "
    }
  ],
  @entrypoint "main.ds",
  @assertions [
    @RunSuccess {
      @result @Int 6
    }
  ]
}
