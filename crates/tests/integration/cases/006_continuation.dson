{
  @files [
    {
      @name "main.ds",
      @content "
        <'string> => <'number>
          <\\'number, 'number -> @sum 'number>
            ! 1 => <'number>,
            ! 2 => <'number>
        $ ! \"a\" => <'number>: <x> ~
        <\\'number, 'number -> @sum 'number> <x>, <x>
      "
    }
  ],
  @entrypoint "main.ds",
  @assertions [
    @RunSuccess {
      @result @Int 6
    }
  ]
}
