{
  @files [
    {
      @name "main.ds",
      @content "
        'type add \\'number, 'number -> @sum 'number
        'string => 'number
          <add>
            ! 1 => 'number,
            ! 2 => 'number
        $ ! \"a\" => 'number: x ~
        <add> <x>, <x>
      "
    }
  ],
  @entrypoint "main.ds",
  @assertions [
    @RunSuccess {
      @result @Int 6
    }
  ]
}
