{
  @files [
    {
      @name "main.ds",
      @content "
        'type add \\'number, 'number -> @sum 'number
        #1 & #2 > \\@dividend 'number, @divisor 'number ->
          ! @quotient 'number ~ @division by zero 'number => 'number
          @dividend 3, @divisor 0 ~
        @division by zero 'number => 'number ->
          >add
            > @division by zero 'number, 1
      "
    }
  ],
  @entrypoint "main.ds",
  @assertions [
    @Typed [
      * 1, @Label {
        @label "quotient",
        @item @Number *
      },
      * 2, @Effectful {
        @ty @Label {
          @label "quotient",
          @item @Number *
        },
        @effects [
          {
            @input @Label {
              @label "division by zero",
              @item @Number *
            },
            @output @Number *
          }
        ]
      }
    ],
    @RunSuccess {
      @result @Int 4
    }
  ]
}
