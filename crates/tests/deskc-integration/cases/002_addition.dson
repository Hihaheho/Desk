{
  @files [
    {
      @name "main.ds",
      @content "
        > \\'number, 'number -> @sum 'number 1, 2.
      "
    }
  ],
  @entrypoint "main.ds",
  @assertions [
    @RunSuccess {
      @result @Number @Integer 3
    }
  ]
}
