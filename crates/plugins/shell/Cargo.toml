[package]
name = "desk-plugin-shell"
version = "0.1.0"
edition = "2018"
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
shell-language = { package = "desk-shell-language", path = "../../systems/shell-language", version = "0.1.0" }
shell-terminal = { package = "desk-shell-terminal", path = "../../systems/shell-terminal", version = "0.1.0" }

plugin-core = { package = "desk-plugin-core", path = "../core", version = "0.1.0" }
terminal = { package = "desk-terminal", path = "../../components/terminal", version = "0.1.0" }
runtime = { package = "desk-runtime", path = "../../components/runtime", version = "0.1.0" }
language = { package = "desk-language", path = "../../components/language", version = "0.1.0" }
physics = { package = "desk-physics", path = "../../components/physics", version = "0.1.0" }

bevy = { version = "0.5", default-features = false }
derivative = "2.2.0"
