[package]
name = "desk-runtime"
version = "0.1.0"
authors = ["Ryo Hirayama <ryo12redstone@gmail.com>"]
edition = "2018"
license = "MIT OR Apache-2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
language = { package = "desk-language", path = "../language", version = "0.1.0" }

uuid = { version = "0.8.2", features = ["serde", "v4", "v5", "wasm-bindgen"] }